{
  "name": "trading-quant-sim",
  "version": "1.0.0",
  "private": true,
  "description": "Repo-level Node scripts used by CI checks",
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "test": "node --test"
  }
}
